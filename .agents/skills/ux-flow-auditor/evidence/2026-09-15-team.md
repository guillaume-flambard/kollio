# Evidence record - Team formation flow audit (2026-09-15)

## Identifiers

- `IdeaDetail.Team.Panel.ReviewApplication` - locales fr, en - scenarios
  TEAM-01, TEAM-02
- `IdeaDetail.Team.Panel.Apply` - locales fr, en - scenarios TEAM-03
- `IdeaDetail.Team.Panel.AddColleague` - locales fr, en - scenarios TEAM-07
- `IdeaDetail.Team.Panel.HideFromMember` - locales fr, en - scenarios TEAM-08

## Scenario result

`apps/web/tests/browser/team.spec.ts`: **10 passed (19.6s)**, single clean run,
one worker. TEAM-01, TEAM-02, TEAM-03, TEAM-07 and TEAM-08 in fr + en. Raw
output: `evidence/2026-09-15-team.spec-output.txt` (non-empty).

- TEAM-01: the owner sees the pending application and an enabled accept button.
- TEAM-02: reject posts a rationale and renders it on the timeline.
- TEAM-07: add-colleague POST matches `{ user_id, participation, function }`.
- TEAM-08: a member sees no add form.

## Grounding checks

- **Locale:** `ideas.detail.team.title`, `.pending`, `.accept`, `.reject`,
  `.rejectReason`, `.addTitle`, `.addParticipation`, `.addFunction`, `.added`,
  `.membersTitle`.
- **Tokens:** `--kollio-active-ink` on actions, `--ui-border`, `--ui-text-muted`.

## Findings

1. **TEAM-03 does not exercise the action it names.** `team.spec.ts:108-114`
   ("lets a member apply to join") only asserts the members heading and the
   `ideas.detail.team.pending` text on the owner-shaped fixture. It never clicks
   an apply control or asserts a join-request POST. The apply path is therefore
   not actually covered; the scenario name overstates the coverage.
2. **No token defect observed** on the tested rendering.

## Verdict

TEAM-01, TEAM-02, TEAM-07 and TEAM-08 AUDITED for fr and en. TEAM-03 is listed
as evidence of rendering only, not of the apply action.
