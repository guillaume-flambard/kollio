# Evidence record - Proposal flow audit (2026-09-15)

## Identifiers

- `Timeline.Proposal.IdeaDetail.Send` - locales fr, en - scenarios PROPOSE-01
- `Timeline.Proposal.IdeaDetail.SurfaceConflict` - locales fr, en - scenarios PROPOSE-02

## Scenario result

`apps/web/tests/browser/proposal.spec.ts`: **4 passed (10.3s)**, single clean
run, one worker. PROPOSE-01 and PROPOSE-02 in fr + en. Raw output:
`evidence/2026-09-15-proposal.spec-output.txt` (non-empty).

- PROPOSE-01: sending a proposal shows the branch line and the pending status.
- PROPOSE-02: a 409 stale-head response surfaces `role="alert"` with the
  conflict copy.

## Grounding checks

- **Locale:** `ideas.iterations.propose`, `ideas.iterations.message`,
  `ideas.iterations.send`, `ideas.iterations.pending`,
  `ideas.iterations.conflict`. All present in fr and en.
- **Tokens:** timeline status styling uses `--kollio-wash`,
  `--kollio-active-ink`, `--kollio-question` (branch marker), `--ui-border`.

## Findings

1. **No findings on the tested paths.** Both pending and conflict states render
   with distinct copy and a real `role="alert"`; no token or locale defect was
   observed.

## Verdict

Both proposal flows AUDITED for fr and en.
