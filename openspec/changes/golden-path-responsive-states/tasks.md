# Tasks

- [x] `responsive.spec.ts`: no horizontal overflow + primary control at 375 and
  768 for the initiative detail (experiments), the onboarding wizard, the
  deposit form and its running narration.
- [x] Fix the phone-width overflow the guard found: settings inputs
  `min-width: 0`; inline and metric create forms stack to one column under 640.
- [x] Confirm empty/loading/error states remain on each screen (unchanged).
- [x] `make verify` and browser suite green (100 checks); this closes #78.
