# Tasks

- [x] `seed_faktus`: deterministic, idempotent Faktus workspace with members,
  full company context (profile, objectives, constraints, principles, metrics)
  and one initiative with its first iteration; `--reset`.
- [x] `make seed-faktus` and `make reset-faktus`.
- [x] Seed integration test: run twice is idempotent; reset leaves nothing.
- [x] Demo path A–I recorded, each step tied to its interface action and the
  executable that proves it.
- [x] §15 acceptance checklist verified with evidence and honest boundaries.
