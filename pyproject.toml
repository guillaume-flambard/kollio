[project]
name = "kollio"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = []

[tool.uv.workspace]
members = [
    "apps/api",
]

[tool.uv]
package = false

[tool.ruff]
extend = "apps/api/pyproject.toml"
